{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q 'git push' && cd /home/user/stats_visualizer && git pull 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
